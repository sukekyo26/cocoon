# Plugins excluded from arm64 e2e because their install script hard-codes
# amd64 or fails fast on arm64. One plugin id per line; blank lines and
# #-comments are ignored. Consumed by .github/workflows/plugin-e2e.yml
# (skips the arm64 matrix entry for these) and by docker-roundtrip.sh's
# PRESET=arm64-full; internal/plugin/e2e_arm64_exclude_test.go guards that
# every id here is a real catalog plugin.
flutter
google-chrome
lazygit
starship
zig
