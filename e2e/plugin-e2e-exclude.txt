# Plugins excluded from the automatic plugin-e2e runs (PR, weekly schedule,
# workflow_dispatch without a plugin) because a build is too heavy for CI —
# e.g. a multi-GB IDE download. Build one on demand with workflow_dispatch
# plugin=<id>. One plugin id per line; blank lines and #-comments are ignored.
# Consumed by .github/workflows/plugin-e2e.yml (detect);
# internal/plugin/e2e_exclude_test.go guards that every id here is a real
# catalog plugin.
android-studio
