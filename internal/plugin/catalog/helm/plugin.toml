[metadata]
name = "Helm"
description = "Kubernetes package manager"
url = "https://github.com/helm/helm"
default = false

[install]
requires_root = true
default_method = "binary"

[install.methods.binary]
description = "Single helm binary extracted from a get.helm.sh tarball"

[version]
version_capable = true
