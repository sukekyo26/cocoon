[metadata]
name = "AWS SAM CLI"
description = "AWS Serverless Application Model CLI"
url = "https://github.com/aws/aws-sam-cli"
default = false

[install]
requires_root = false
default_method = "archive"

[install.methods.archive]
description = "Extract vendored AWS SAM CLI zip and run sam-installation/install"

[version]
version_capable = false
