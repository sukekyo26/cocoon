[metadata]
name = "Google Chrome"
description = "Google Chrome stable browser for headless testing with Playwright/Puppeteer"
url = "https://www.google.com/chrome/"
default = false

[install]
requires_root = true
default_method = "apt"

[install.methods.apt]
description = ".deb package fetched from dl.google.com and installed via apt"

[version]
version_capable = false
