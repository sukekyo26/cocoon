[metadata]
name = "Starship"
description = "Cross-shell prompt (https://github.com/starship/starship)"
default = false

[install]
requires_root = true
volumes = ["/home/${USERNAME}/.config"]

[version]
version_capable = true
