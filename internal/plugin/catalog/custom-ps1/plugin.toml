[metadata]
name = "Custom PS1"
description = "Custom bash prompt with Docker container name, git status, and colorized output"
default = false
conflicts = ["starship"]

[install]
requires_root = false

[version]
version_capable = false
