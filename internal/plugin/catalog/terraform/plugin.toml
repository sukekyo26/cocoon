[metadata]
name = "Terraform"
description = "Infrastructure as Code by HashiCorp (https://github.com/hashicorp/terraform)"
default = false

[apt]
packages = ["unzip"]

[install]
requires_root = true

[version]
version_capable = true
