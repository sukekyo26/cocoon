[metadata]
name = "Docker CLI"
description = "Docker CLI client for using host Docker daemon via socket mount"
url = "https://github.com/docker/cli"
default = true

[install]
requires_root = true
build_args = ["DOCKER_GID"]

[version]
version_capable = false
