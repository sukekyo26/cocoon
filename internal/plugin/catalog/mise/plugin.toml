[metadata]
name = "mise"
description = "Polyglot runtime version manager"
url = "https://github.com/jdx/mise"
default = false

[install]
requires_root = false
volumes = ["/home/${USERNAME}/.mise"]
default_method = "installer"

[install.methods.installer]
description = "Upstream mise.run installer script piped to sh"

[install.env]
MISE_DATA_DIR = "/home/${USERNAME}/.mise"
PATH = "/home/${USERNAME}/.local/bin:$PATH"

[version]
version_capable = true
