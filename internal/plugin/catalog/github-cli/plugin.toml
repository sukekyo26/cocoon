[metadata]
name = "GitHub CLI"
description = "GitHub command line tool (https://github.com/cli/cli)"
default = false

[install]
requires_root = true
volumes = ["/home/${USERNAME}/.config"]

[version]
version_capable = false
