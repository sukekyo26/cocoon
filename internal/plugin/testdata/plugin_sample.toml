[metadata]
name = "Sample"
description = "Sample plugin"
url = "https://example.com/sample"
default = false

[install]
requires_root = true
volumes = ["/home/${USERNAME}/.sample"]
default_method = "installer"

[install.methods.installer]
description = "Sample installer (test fixture)"

[install.env]
SAMPLE_HOME = "/home/${USERNAME}/.sample"

[version]
version_capable = false
